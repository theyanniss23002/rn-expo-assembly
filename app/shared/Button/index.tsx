import React from 'react';
import { Text, View } from 'react-native';

const Button: React.FC = () => {
    return (
        <View>
            <Text>There will be a button to reuse</Text>
        </View>
    );
};

export default Button;
